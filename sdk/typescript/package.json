{
  "name": "@omem/sdk",
  "version": "0.1.0",
  "description": "OMEM SDK: trustworthy memory for AI agents",
  "type": "module",
  "//": "private until there is a build step. `main` points at raw TypeScript, so publishing this today would ship a package that breaks on import for every plain-JS consumer, under a name that is then taken. Remove `private` in the same change that adds a build.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "homepage": "https://infrastructure.omem-cloud.com",
  "repository": { "type": "git", "url": "git+https://github.com/troybrandonc-bit/Omem.git", "directory": "sdk/typescript" },
  "bugs": { "url": "https://github.com/troybrandonc-bit/Omem/issues" },
  "keywords": ["ai", "agents", "memory", "llm", "mcp", "belief-revision", "provenance"]
}
